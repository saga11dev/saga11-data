---
permalink: '/post.json'
---
{
	"posts": [
		{% for item in collections.post %}
			{
				"title": "{{ item.data.title }}",
				"url": "{{ item.url }}",
				"content": "{{ item.content }}"

			}{% if not loop.last %},{% endif %}
		{% endfor %}
	]
}
